# CROWN-MCPP 论文需求—实现—验证审计矩阵

本文件以 `articles/多智能体顶刊核心算法.md` 为唯一算法规范。状态只有“完成、进行中、缺失”，不以兼容接口或最小示例替代论文要求。

| 论文要求 | 权威实现证据 | 验证证据 | 当前状态 |
|---|---|---|---|
| 固定、内部不重叠责任单元 | `crown/geometry.py` 对任务多边形减去膨胀障碍后的连续自由空间执行垂直事件 BCD；真实多边形是责任语义，`bounds` 不再充当狭缝包络 | 对抗性斜三角障碍及 15×15 真实图均强制验证 `gap_area=spill_area=overlap_area=0` | 完成 |
| 异构单元多模式及覆盖/运动学验证 | `crown/mode_graph.py` 按 agent profile 生成扫掠方向、顺序、入口/出口模式并逐个验证覆盖、动力学、障碍、电量和时间 | 小型几何端到端测试、到达时刻海流重定时测试 | 完成（有限模式库） |
| 覆盖与安全误差裕度 | `crown/geometry.py`、`mode_graph.py`、`motion.py` 实施连续扫掠矩形差集证书、有效足迹收缩、规划距离扩张和保守管 | 子网格边界缺口拒绝测试、有效覆盖、管栅格和端到端覆盖断言 | 完成 |
| 障碍感知连接运动原语 | `mode_graph.py` 懒缓存 Dubins/A*/运动格连接并按出发时刻重定时；首解的直连探测失败只记为“需绕行”，不再误判不可达 | 小型端到端及 15×15 矩形+三角障碍真实图全局可行路线回归 | 完成 |
| 保守连续时空管及规范资源映射 | `motion.py` 栅格化扫掠管；`conflicts.py` 连续最小距离及规范资源映射 | 连续交叉冲突映射测试 | 完成（线性有限原语） |
| 时间扩展多模式图 | `mode_graph.py`、`pricing.py` 显式等待、保守向上取整、时变流服务/连接重定时 | 等待占用、到达时刻海流和 BPC 测试 | 完成 |
| 完整路线列编码分配/顺序/模式/时间/能量 | `types.py`、`pricing.py` 的 `CrownTimedRoute` 编码任务、模式、原语、时隙、资源和能量 | BPC/ENUM 与图 oracle 测试 | 完成 |
| 电量、最大任务时间和返航 | `AgentPlanningProfile`、图定价和显式/默认终点连接 | 配置与小实例回归覆盖；专门边界测试仍可增加 | 完成 |
| 严格词典序主问题 | `bpc.py` 和 `graph_bpc.py` 均执行 makespan 后固定最优 makespan 的能耗阶段 | 独立 ENUM 对拍 | 完成（有限模型） |
| 标签式元素资源约束精确定价与安全支配 | `pricing.py` 保留完整已访问集合、能量、时间、模式节点、分支和资源对偶价 | 图 BPC 与独立 oracle 一致 | 完成（指数级精确定价） |
| 惰性冲突资源分离且割后重新定价 | `graph_bpc.py` 分离离散容量与连续管冲突，激活割后重新执行列生成 | 共享航道、连续交叉和图 BPC 测试 | 完成 |
| 任务—智能体、相邻和资源先后兼容分支 | `graph_bpc.py` 实现任务—智能体、相邻、规范资源首次占用先后及完备路线变量后备分支；先后分支行对偶由 `pricing.py` 精确定价 | 共享航道中实际触发资源先后分支；首次进入对偶系数测试；最终与独立连续 ENUM 一致 | 完成 |
| CROWN-BPC 有限模型词典序全局最优 | `graph_bpc.py` 图定价、分支、割和两阶段求解 | 3 智能体共享航道与独立连续 ENUM oracle 一致，Gap=0 | 完成（声明限于固定单元/模式/时格/时域） |
| CROWN-LNS 四类核心邻域 | `lns.py` 任务链迁移、链交换、模式/端点重定价、冲突资源重规划；所有权—顺序—模式联合首解允许障碍绕行后验证 | 合成联合连通任务归属测试及 15×15 真实障碍图连续责任、全任务、有限 UB 回归 | 完成 |
| 对偶引导与路线池整数重组 | 根 CG/路线池 LP 对偶引导，定期 DFS 集合划分重组；受限池值不冒充下界 | LNS 下界有效性测试 | 完成 |
| 服务工作量下界 | `service_workload_lower_bound` 求定理 7 的连续分配 LP；时变流不安全时退化为 0 | 合成实例精确值测试 | 完成 |
| 根列生成下界、UB/LB/Gap anytime 证书 | LNS 报告 `max(LB_service, LB_CG)`、可行 UB、Gap 和时间轨迹；根精确定价有独立预算切片 | LNS 证书测试 | 完成（根定价未收敛时仅用服务下界） |
| 现有顺序路线作为初始列/保底 | `baseline.py` 先做服务负载固定分配；若该分配在障碍图不连通，则在不使用冲突价格的前提下只修复所有权/顺序/模式，再执行优先消冲突并送入路线池 | 构造性严格改进测试；15×15 真实图生成顺序基线并由 LNS 报告 `baseline_makespan` | 完成 |
| `PathPlanningLayer` 中完整 CROWN 规划器 | `algorithms/crown_mcpp_planner.py` 已注册；配置、适配器、报告和 CLI 已接入 | 小型真实几何端到端测试 | 完成 |
| 地图—舰队物理配置与直接运行 | 七张内置地图各自声明独立 profile；覆盖足迹、物理艇体和转弯半径分离；手工 CLI 与 profile loader 均禁止以覆盖足迹回退艇体 | profile 绑定/缺失艇体单测；10/15/20/50/两张 200/400 门控端到端回归；预算×重复实验驱动 | 完成（配置与入口）；规模统计见 `CROWN-全地图直接运行与规模实验.md` |
| 精确/认证启发式/纯启发式状态区分 | 输出区分 `exact_graph_bpc`、根精确定价 LNS 和仅服务下界 LNS | 状态及证书测试 | 完成（当前无“无下界纯启发式”状态） |
| 论文指标、消融和规模化实验入口 | `full_experiment.py` 输出证书、路线图和 anytime 图；共享航道证明入口完整 | 十基线、30 次统计、200—2000 单元规模实验尚未实现 | 进行中 |

只有本表所有论文算法项均为“完成”，对应测试、运行产物和声明边界全部通过审计后，才能宣称“完整实现整个 CROWN-MCPP”。

当前回归基线：`python -m unittest discover -s tests` 共运行 176 项，169 项通过，7 项是需显式设置 `CROWN_RUN_MAP_REGRESSION=1` 的昂贵地图门控用例。本轮另通过 `examples/run_crown_map_suite.py` 实际完成了七张内置地图的端到端运行，两张 200 米图和 400 米图还完成了 60 秒与长预算的最小对照。除第 14 节规定的外部基线、30 次统计与 200—2000 单元规模化实验外，本表中的算法实现项均已有代码与自动回归证据；在这些论文级实验完成前，仍不能宣称“整篇顶刊论文验证已经完成”。
